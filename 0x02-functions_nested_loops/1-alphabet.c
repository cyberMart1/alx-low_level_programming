#include "main.h"

/**
 * main - check the code                        *                                              * Return: Always 0.
 */                                            int main(void)                                 {                                                      char letter;                                   for (letter = 'a'; letter <= 'z'; letter++)
                print_alphabet(letter);                return (0);
}
