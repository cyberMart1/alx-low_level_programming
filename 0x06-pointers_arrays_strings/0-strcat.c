#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

strcat(char *dest, const char *src)
{
	char i[] = "Hello ";
	char j[] = "World!\n";
	strcat(i,j);
	_putchar("%s\n",i);
	return (dest);
}
